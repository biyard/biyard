export interface Service {
  id: string;
  title: string;
  description: string;
  link?: string;
  image: string;
}

export interface ApproachItem {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface CoreValue {
  id: string;
  title: string;
  description: string;
  icon: string;
}
